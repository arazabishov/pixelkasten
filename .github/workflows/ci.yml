name: CI

on:
  push:
    branches: [trunk]
  pull_request:
    branches: [trunk]

jobs:
  test:
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v5
        with:
          version: "latest"
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install exiftool (Ubuntu)
        if: runner.os == 'Linux'
        run: sudo apt-get install -y libimage-exiftool-perl
      - name: Install exiftool (Windows)
        if: runner.os == 'Windows'
        run: choco install exiftool -y
      - run: uv sync
      - name: Unit tests
        run: uv run pytest packages/core/tests/unit/ -v
      - name: Integration tests
        run: uv run pytest packages/core/tests/integration/ -v

  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v5
        with:
          version: "latest"
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - run: uv sync
      - name: Ruff check
        run: uv run ruff check packages/
      - name: Ruff format check
        run: uv run ruff format --check packages/
      - name: Type check
        run: uv run pyright
